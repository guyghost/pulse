/**
 * Barrel for dashboard state models (source of truth consumed by UI + shell).
 * See individual files for invariants and tests.
 */
export * from './dashboard-surface.model';
export * from './metrics-visibility.model';
export * from './batch-selection.machine';
